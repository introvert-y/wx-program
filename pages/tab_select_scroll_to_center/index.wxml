<!-- 横向滚动菜单-选中标题居中显示 -->

<view class="header">横向滚动菜单-选中标题居中显示</view>
<scroll-view
  class="main"
  scroll-left="{{scrollLeft}}"
  scroll-with-animation
  scroll-x
>
<view class="box">
  <view
    wx:for="{{tabmenu}}"
    wx:key="index"
    class="item {{crtTab === index ?'active':''}} "
    id="item_{{index}}"
    data-index="{{index}}"
    bindtap='onMove'
  >
    <!-- <view class="backgroup_animate"></view> -->
    {{item.name}}
    <view wx:if="{{crtTab === index}}" class="bottom_line" ></view>
  </view>
</view>
</scroll-view>
